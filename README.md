# hybrid-boilerplate
```
# dependencies
npm install -g bower gulp-cli cordova ionic
npm install
bower install
# build src -> www, mobile/www
gulp build
# serve api and web
gulp serve
# create ionic app
ionic create mobile
# ... look at ionic website
ionic serve
